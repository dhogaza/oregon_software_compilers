
-- random stuff

   -- Sacrificing sl for use as a regparam for now as the allocator in travrs doesn't
      know about gotos that require the static link on aarch64. Or perhaps the goto code
      should be smarter about top-level procedures so it doesn't eat the static link for
      top-level procedures (which is what the mc68000 version and all the other codegens
      I no longer have access to did).  Regardless, with so many registers, this is a
      minor point, more aesthetic than important.
---

*** current bugs

   -- the fp offset issue attemped (?) fix might be wrong (or right).  The
      spoffset used to set fp should merely be the amount subtracted from the
      stack on entry -blocksize or something like that.  Think a bit.

   -- 64-bit arithmetic doesn't work, not suprisingly.

   -- sets with > 8 elements and < 17 elements are allocated 64 bits.  No 16 or 32
      bits probably because of copying allocation stuff from mc68000 mda. More curious
      than crucial.  But we really should be able to do length/align 1/1, 2/2, 4/4 and
      8/8 sets.

  -- set arithmetic with the empty set fails unless the other set oprnds are 32 bit.
     front end limitation.

  -- regenoprnd stuff all messed up? adrp and makeaddressable not only for global level?

  -- varop nodes for reg parameters all have level 0, offset 0 and has got to screw
     up some stuff???  Certainly makes it harder to assign to regtemp.

--- current work

  -- procedure params

     writeprocref(procno, offset): either write .P label or the procname for an
     external.  Will make writing proclabel simpler.

     proclabel is like datalabel but with proc reference.

  -- std routine parameters

     pushing of strings -> moving strings to param registers ugh...

     alloc one param for the length and then if regparam do some hacked up
     shit to get the length?  Target code something like:

     move address to Xn+1
     mov [Xn+1,1], Xn
     add xn, xn, 1

     should be able to do this in body ... maybe helper for the len

  -- FILES

    -- Compiler

     main initiation:

	adrp	x0, :got:stdout
	ldr	x0, [x0, :got_lo12:stdout]


     Binary file stuff should be easier in the compiler.

     Doing this based on streams would seem to make the most sense though fds would
     probably be easier.

    -- Library

     This is going to be complex.  Figuring how to represent files is going to be
     quite the trip on its own. The library code can probably be written in Pascal
     interfacing to the C library.  Or the C library more likely ...

  --

     Make special empty set integer constant and make movsetx deal with it rather
     than generate it in the string file.  This will be needed when type:[set const]
     extension. Conditionalize it as aarch64.

     test for case where short set variables are assigned regtemps due
     to frequent use.  Might generate some lousy code with extra moves?

     -- do for simplicity and clarity 

     break handle_offset9_oprnd into two again?  The idea is that we want a general 
     handle_abstract_offset_oprnd procedure that will handle neg offsets as signed_offset
     and positive as unsigned_offset.

     LDP/STP just handle as special case and is always a signed_offset.

     Treat post/preindex as aborts if they are out of the signed range.

     Reg allocation has to get away from allocating 
     essentially exclusively to caller-saved regs but everything should be tested
     thoroughly before increasing use of callee-saved because it hammers register
     spilling, operand regeneration without using saved registers, etc.

    [reg, reg] take advantage of shifting when possible, i.e. load 16 bit
    constant then shift a bit to the left (by the length of the data being
    referenced?) to save an instruction.

-- next priorities:

0.1 Alignment and think about unaligned addressing which appears to work 
    except another thread can walk on you in between two parts being read
    or written if you cross a long boundary.  Investigate.  Track basic
    alignment in code generator much as "knowneven" in the mc68K codegen.

0.2 improve vs pushfinal which no longer exists (simpleinvar), need
    to add a finalop to replace pushfinal?  This is used to designate
    an invariant final limit ...

    Needs testing, it might actually work as is.

0.3
    align variables, too.

    unless in packed structure of course.

    This makes using 2, 4, 8, 16 byte moves possible for structs.

0.4 fix up lastlabel/newlabel/definelastlabel bullshit

2. improve for loops/fix

   a. if a register is loaded before the top compare and after
      last branch (boolean expressions in first or last expression),
      mark it.  It doesn't need to be reloaded at the bottom.

4.  yet to be figured out how to put the reg return value forced
   to physical reg 0 in leaf functions if allocated as a valuable
   regtemp by improve?  Or is this really a viable
   idea if param 0 is being used?

9. lots of sign extension and the like issues?

10.targeting logic sucks on this machine, too many instances
   of ld xn, mem/op xm,xn/mov xn,xm/ when xm is no longer useful.

11. block moves should just use memcpy for now.  optimizing these is
    very low on the priority list.

-- more general

1. if current block makes no calls allocate regtemps to
   caller-saved registers.  Better, track calls to procs
   including intrinsics and determine which scratch registers
   are never used by called procedures in a block and 
   allocate those first so we can avoid saving them.  Track
   by context stack with more logic in travrs?

2. Need to allow parameters that aren't required to be in
   memory for nested procs to be assigned true regtemps
   as frequent use of params in the context of many calls
   to procs leads to horrendous number of spills

3. what's with cseregions being zero?  bug in the code?  Try it! (config)

4. hoist global dolevels if they are inserted normally.


11. STACK PARAMETERS MUST NOT BE ADDED TO BLOCK'S SIZE FOR SP ADJUST

Register parameters:

1. Complications include short structs that are passed in two registers.
   GCC passes the struct on the stack if there aren't two variables left.

2, Huge complication regarding records with uniform float/double fields that
   will fit in the parameter registers.

Future enhancements to improve:

1. This will require new pseudoops with some way to pass the add/sub
   operand along with the normal left/right ones, preferably without
   losing targeting.

   a) combine multiply followed by add or subtract into MADD/MSUB
   b) combine multiply followed by aindx with length of 1 (no scaling)
      into addrop+MADD/MSUB.
   c) don't forget the MNEG instruction!

2. flatten expressions with comparisions

n-1: find uses for all sorts of weird instructions
n. vectorization because that was something I always wanted to do ...

Peephole needs:

1. Two register loads from consecutive memory address converted to
   ldp insruction.

2. cmp xn,0; b.cond l -> cbz x0,l, cond and associated thingies

FAILS (for now at least)

0.1 It appears that the [Xr, :lo12:xyz+n] does the right
    thing for all ldr/str [long, word, half, byte].  However
    the data must be aligned correctly, obviously.

    using regenoprnd to reconstruct bss memory operands orks fine but
    the original adp used to generate the index is not, of course, deleted.
    Perhaps a new keyx field "addressed" and if the proper
    kind of key value is marked but not addressed, the dangling adrp can be
    deleted.

    Minor is that globals should be indexed before aindx with the variable
    base for this scheme, 'way back in body.pas.  Conditionalized for aarch64
    if course.

-- minor optimization reminders

1. var +/- intconst +/- intconst doesn't fold, though the documentation in
   improve claims it does so.  Blaim Steve from Eclipse? :)
